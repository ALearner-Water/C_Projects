# 第 0 次诊断

日期：2026-09-01  
要求：闭卷，不搜索，不运行代码；不会就写“不知道”。本次不计成绩。

## 题目

1. 数据的“逻辑结构”和“存储结构”有什么区别？

2. 数组为什么能通过下标快速访问元素？

3. 在长度为 `n` 的顺序表开头插入一个元素，大约需要移动多少个原有元素？

4. 单链表节点中的 `next` 保存的是什么？

5. 判断下面程序段的时间复杂度，并简单说明原因。

```c
for (int i = 0; i < n; i++) {
    for (int j = 0; j < i; j++) {
        count++;
    }
}
```

## 回答区

1.

2.

3.

4.

5.
