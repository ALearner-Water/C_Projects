# 2026-09-02 学习任务

预计总用时：60-75 分钟。按顺序完成，核心任务结束后即可停止。

## 任务 1：验收 `max_value`（15 分钟）

当前代码已经可以编译。今天完成以下改进：

1. 把函数名 `max` 改为 `max_value`。
2. 把局部变量 `max` 改为 `largest`，避免同名造成阅读混乱。
3. 因为 `largest` 已经取了 `arr[0]`，循环从 `i = 1` 开始。
4. 分别测试：

```text
{1, 2, 3, 4, 5} -> 5
{-5, -2, -9}     -> -2
{-7}              -> -7
```

验收标准：三个结果全部正确，编译时没有警告。

## 任务 2：第 1 章短视频预习（30 分钟）

课程：[王道计算机考研《数据结构》](https://www.bilibili.com/video/BV1b7411N798/)

观看：

- `1.0 开篇：数据结构在学什么`（约 8 分钟）
- `1.1 数据结构的基本概念`（约 22 分钟）

观看前带着三个问题：

1. 数据结构研究哪三方面？
2. 逻辑结构和存储结构之间是什么关系？
3. 一个算法和一个程序是不是完全相同的概念？

看完后关闭视频，不看笔记，用自己的话回答前两个问题。第三问可以暂时保留到复杂度课程。

## 任务 3：指针复健起步（20-30 分钟）

先写出下面各表达式的含义：

```c
int x = 10;
int *p = &x;
```

- `x`：
- `&x`：
- `p`：
- `*p`：
- `&p`：

然后从空白重新实现：

```c
void swap(int *a, int *b);
```

并用 `x = 3`、`y = 8` 测试。调用后应得到 `x = 8`、`y = 3`。

如果完全想不起来，先看主讲课程第 2 集中的“指针”部分，看到进入结构体内容时停止，再回来重写。

## 今日完成标准

- `max_value` 通过三个测试。
- 完成两段第 1 章短视频并能闭卷复述。
- 能区分 `p` 与 `*p`，并完成或尝试完成 `swap`。

不要求今天学习结构体、动态内存或 LeetCode。
